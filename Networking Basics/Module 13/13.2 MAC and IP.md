<!-- START doctoc generated TOC please keep comment here to allow auto update -->
<!-- DON'T EDIT THIS SECTION, INSTEAD RE-RUN doctoc TO UPDATE -->
**Table of Contents**  *generated with [DocToc](https://github.com/thlorenz/doctoc)*

- [Destination on the Same Network & Address Resolution](#destination-on-the-same-network--address-resolution)
  - [Two Types of Addresses on an Ethernet LAN](#two-types-of-addresses-on-an-ethernet-lan)
  - [What Happens When the Destination Is on the Same Network](#what-happens-when-the-destination-is-on-the-same-network)
- [Destination on a Remote Network](#destination-on-a-remote-network)

<!-- END doctoc generated TOC please keep comment here to allow auto update -->

## Destination on the Same Network & Address Resolution

- A host may need to send a message but only knows the destination IP address.
- To actually deliver the frame on an Ethernet network, the host must also know the destination MAC address.
- This is why address resolution is required.

### Two Types of Addresses on an Ethernet LAN

- **MAC Address (Physical Address)**  
  Used for NIC-to-NIC communication on the same local network.

- **IP Address (Logical Address)**  
  Used to identify the source and destination devices at the network layer.  
  The destination IP may be on the same network or a remote network.

### What Happens When the Destination Is on the Same Network

- Layer 2 MAC addresses are used to deliver the Ethernet frame.
- If the destination IP is on the same network, then:
  - The destination MAC address is the MAC of the target device itself
  - The frame is sent directly from source NIC to destination NIC
>[!Example] 
>Consider the following example using simplified MAC Address representations
>![MAC Address Representation](/Imagesn/103.png)
> PC1 wants to send a packet to PC2. The figure displays the Layer 2 destination and source MAC addresses and the Layer 3 IPv4 addressing that would be included in the packet sent from PC1.
>
>The Layer 2 Ethernet frame contains the following:
>
>- **Destination MAC address** – This is the simplified MAC address of PC2, `55-55-55`.
>- **Source MAC address** – This is the simplified MAC address of the Ethernet NIC on PC1, `aa-aa-aa`.
>
>The Layer 3 IP packet contains the following:
>
>- **Source IPv4 address** – This is the IPv4 address of PC1, 192.168.10.10.
>- **Destination IPv4 address** – This is the IPv4 address of PC2, 192.168.10.11.

___
 
## Destination on a Remote Network

When the destination IP address (IPv4 or IPv6) is on a remote network, the destination MAC address will be the address of the host default gateway (i.e., the router interface).